function displayUsers(users){
    //travel the array
    
    users.forEach(user => {
        
    });
    //get each user

    //create th tr

    //append the tr to the table

}

function init(){
    console.log('Listing users');
    let arrayUsers = readUser();
    displayUsers(arrayUsers)
}

window.onload=init;